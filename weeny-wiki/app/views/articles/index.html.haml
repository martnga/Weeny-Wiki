
-@articles.each do |article|
  %h2= link_to article.title, article
  %p= truncate(article.content, length: 200)
  %p
    Published on
    = article.created_at.strftime('%A, %d/%m/%Y')

- if user_signed_in?
  =link_to "New Article", new_article_path
